using System.Windows;

namespace WaferInspectionTool
{
    // Main window for the wafer inspection tool. All logic is handled in the ViewModel (MVVM).
    public partial class MainWindow : Window
    {
        public MainWindow()
        {
            InitializeComponent();
        }
    }
}